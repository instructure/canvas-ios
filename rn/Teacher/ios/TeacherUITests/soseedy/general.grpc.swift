/*
 * DO NOT EDIT.
 *
 * Generated by the protocol buffer compiler.
 * Source: general.proto
 *
 */

/*
 * Copyright 2018, gRPC Authors All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
import Foundation
import Dispatch
import gRPC
import SwiftProtobuf

/// Type for errors thrown from generated client code.
internal enum Soseedy_SeedyGeneralClientError : Error {
  case endOfStream
  case invalidMessageReceived
  case error(c: CallResult)
}

/// GetHealthCheck (Unary)
internal class Soseedy_SeedyGeneralGetHealthCheckCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/soseedy.SeedyGeneral/GetHealthCheck")
  }

  /// Run the call. Blocks until the reply is received.
  fileprivate func run(request: Soseedy_HealthCheckRequest,
                       metadata: Metadata) throws -> Soseedy_HealthCheck {
    let sem = DispatchSemaphore(value: 0)
    var returnCallResult : CallResult!
    var returnResponse : Soseedy_HealthCheck?
    _ = try start(request:request, metadata:metadata) {response, callResult in
      returnResponse = response
      returnCallResult = callResult
      sem.signal()
    }
    _ = sem.wait(timeout: DispatchTime.distantFuture)
    if let returnResponse = returnResponse {
      return returnResponse
    } else {
      throw Soseedy_SeedyGeneralClientError.error(c: returnCallResult)
    }
  }

  /// Start the call. Nonblocking.
  fileprivate func start(request: Soseedy_HealthCheckRequest,
                         metadata: Metadata,
                         completion: @escaping (Soseedy_HealthCheck?, CallResult)->())
    throws -> Soseedy_SeedyGeneralGetHealthCheckCall {

      let requestData = try request.serializedData()
      try call.start(.unary,
                     metadata:metadata,
                     message:requestData)
      {(callResult) in
        if let responseData = callResult.resultData,
          let response = try? Soseedy_HealthCheck(serializedData:responseData) {
          completion(response, callResult)
        } else {
          completion(nil, callResult)
        }
      }
      return self
  }

  /// Cancel the call.
  internal func cancel() {
    call.cancel()
  }
}

/// SeedData (Unary)
internal class Soseedy_SeedyGeneralSeedDataCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/soseedy.SeedyGeneral/SeedData")
  }

  /// Run the call. Blocks until the reply is received.
  fileprivate func run(request: Soseedy_SeedDataRequest,
                       metadata: Metadata) throws -> Soseedy_SeededData {
    let sem = DispatchSemaphore(value: 0)
    var returnCallResult : CallResult!
    var returnResponse : Soseedy_SeededData?
    _ = try start(request:request, metadata:metadata) {response, callResult in
      returnResponse = response
      returnCallResult = callResult
      sem.signal()
    }
    _ = sem.wait(timeout: DispatchTime.distantFuture)
    if let returnResponse = returnResponse {
      return returnResponse
    } else {
      throw Soseedy_SeedyGeneralClientError.error(c: returnCallResult)
    }
  }

  /// Start the call. Nonblocking.
  fileprivate func start(request: Soseedy_SeedDataRequest,
                         metadata: Metadata,
                         completion: @escaping (Soseedy_SeededData?, CallResult)->())
    throws -> Soseedy_SeedyGeneralSeedDataCall {

      let requestData = try request.serializedData()
      try call.start(.unary,
                     metadata:metadata,
                     message:requestData)
      {(callResult) in
        if let responseData = callResult.resultData,
          let response = try? Soseedy_SeededData(serializedData:responseData) {
          completion(response, callResult)
        } else {
          completion(nil, callResult)
        }
      }
      return self
  }

  /// Cancel the call.
  internal func cancel() {
    call.cancel()
  }
}

/// Call methods of this class to make API calls.
internal class Soseedy_SeedyGeneralService {
  public var channel: Channel

  /// This metadata will be sent with all requests.
  internal var metadata : Metadata

  /// This property allows the service host name to be overridden.
  /// For example, it can be used to make calls to "localhost:8080"
  /// appear to be to "example.com".
  internal var host : String {
    get {
      return self.channel.host
    }
    set {
      self.channel.host = newValue
    }
  }

  /// Create a client.
  internal init(address: String, secure: Bool = true) {
    gRPC.initialize()
    channel = Channel(address:address, secure:secure)
    metadata = Metadata()
  }

  /// Create a client that makes secure connections with a custom certificate and (optional) hostname.
  internal init(address: String, certificates: String, host: String?) {
    gRPC.initialize()
    channel = Channel(address:address, certificates:certificates, host:host)
    metadata = Metadata()
  }

  /// Synchronous. Unary.
  internal func gethealthcheck(_ request: Soseedy_HealthCheckRequest)
    throws
    -> Soseedy_HealthCheck {
      return try Soseedy_SeedyGeneralGetHealthCheckCall(channel).run(request:request, metadata:metadata)
  }
  /// Asynchronous. Unary.
  internal func gethealthcheck(_ request: Soseedy_HealthCheckRequest,
                  completion: @escaping (Soseedy_HealthCheck?, CallResult)->())
    throws
    -> Soseedy_SeedyGeneralGetHealthCheckCall {
      return try Soseedy_SeedyGeneralGetHealthCheckCall(channel).start(request:request,
                                                 metadata:metadata,
                                                 completion:completion)
  }
  /// Synchronous. Unary.
  internal func seeddata(_ request: Soseedy_SeedDataRequest)
    throws
    -> Soseedy_SeededData {
      return try Soseedy_SeedyGeneralSeedDataCall(channel).run(request:request, metadata:metadata)
  }
  /// Asynchronous. Unary.
  internal func seeddata(_ request: Soseedy_SeedDataRequest,
                  completion: @escaping (Soseedy_SeededData?, CallResult)->())
    throws
    -> Soseedy_SeedyGeneralSeedDataCall {
      return try Soseedy_SeedyGeneralSeedDataCall(channel).start(request:request,
                                                 metadata:metadata,
                                                 completion:completion)
  }
}


/// Type for errors thrown from generated server code.
internal enum Soseedy_SeedyGeneralServerError : Error {
  case endOfStream
}

/// To build a server, implement a class that conforms to this protocol.
internal protocol Soseedy_SeedyGeneralProvider {
  func gethealthcheck(request : Soseedy_HealthCheckRequest, session : Soseedy_SeedyGeneralGetHealthCheckSession) throws -> Soseedy_HealthCheck
  func seeddata(request : Soseedy_SeedDataRequest, session : Soseedy_SeedyGeneralSeedDataSession) throws -> Soseedy_SeededData
}

/// Common properties available in each service session.
internal class Soseedy_SeedyGeneralSession {
  fileprivate var handler : gRPC.Handler
  internal var requestMetadata : Metadata { return handler.requestMetadata }

  internal var statusCode : Int = 0
  internal var statusMessage : String = "OK"
  internal var initialMetadata : Metadata = Metadata()
  internal var trailingMetadata : Metadata = Metadata()

  fileprivate init(handler:gRPC.Handler) {
    self.handler = handler
  }
}

// GetHealthCheck (Unary)
internal class Soseedy_SeedyGeneralGetHealthCheckSession : Soseedy_SeedyGeneralSession {
  private var provider : Soseedy_SeedyGeneralProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Soseedy_SeedyGeneralProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Soseedy_HealthCheckRequest(serializedData:requestData)
        let replyMessage = try self.provider.gethealthcheck(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// SeedData (Unary)
internal class Soseedy_SeedyGeneralSeedDataSession : Soseedy_SeedyGeneralSession {
  private var provider : Soseedy_SeedyGeneralProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Soseedy_SeedyGeneralProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Soseedy_SeedDataRequest(serializedData:requestData)
        let replyMessage = try self.provider.seeddata(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}


/// Main server for generated service
internal class Soseedy_SeedyGeneralServer {
  private var address: String
  private var server: gRPC.Server
  private var provider: Soseedy_SeedyGeneralProvider?

  /// Create a server that accepts insecure connections.
  internal init(address:String,
              provider:Soseedy_SeedyGeneralProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    self.server = gRPC.Server(address:address)
  }

  /// Create a server that accepts secure connections.
  internal init?(address:String,
               certificateURL:URL,
               keyURL:URL,
               provider:Soseedy_SeedyGeneralProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    guard
      let certificate = try? String(contentsOf: certificateURL, encoding: .utf8),
      let key = try? String(contentsOf: keyURL, encoding: .utf8)
      else {
        return nil
    }
    self.server = gRPC.Server(address:address, key:key, certs:certificate)
  }

  /// Start the server.
  internal func start(queue:DispatchQueue = DispatchQueue.global()) {
    guard let provider = self.provider else {
      fatalError() // the server requires a provider
    }
    server.run {(handler) in
      print("Server received request to " + handler.host
        + " calling " + handler.method
        + " from " + handler.caller
        + " with " + String(describing:handler.requestMetadata) )

      do {
        switch handler.method {
        case "/soseedy.SeedyGeneral/GetHealthCheck":
          try Soseedy_SeedyGeneralGetHealthCheckSession(handler:handler, provider:provider).run(queue:queue)
        case "/soseedy.SeedyGeneral/SeedData":
          try Soseedy_SeedyGeneralSeedDataSession(handler:handler, provider:provider).run(queue:queue)
        default:
          break // handle unknown requests
        }
      } catch (let error) {
        print("Server error: \(error)")
      }
    }
  }
}

