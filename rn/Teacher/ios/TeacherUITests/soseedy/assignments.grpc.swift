/*
 * DO NOT EDIT.
 *
 * Generated by the protocol buffer compiler.
 * Source: assignments.proto
 *
 */

/*
 * Copyright 2018, gRPC Authors All rights reserved.
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
import Foundation
import Dispatch
import gRPC
import SwiftProtobuf

/// Type for errors thrown from generated client code.
internal enum Soseedy_SeedyAssignmentsClientError : Error {
  case endOfStream
  case invalidMessageReceived
  case error(c: CallResult)
}

/// CreateAssignment (Unary)
internal class Soseedy_SeedyAssignmentsCreateAssignmentCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/soseedy.SeedyAssignments/CreateAssignment")
  }

  /// Run the call. Blocks until the reply is received.
  fileprivate func run(request: Soseedy_CreateAssignmentRequest,
                       metadata: Metadata) throws -> Soseedy_Assignment {
    let sem = DispatchSemaphore(value: 0)
    var returnCallResult : CallResult!
    var returnResponse : Soseedy_Assignment?
    _ = try start(request:request, metadata:metadata) {response, callResult in
      returnResponse = response
      returnCallResult = callResult
      sem.signal()
    }
    _ = sem.wait(timeout: DispatchTime.distantFuture)
    if let returnResponse = returnResponse {
      return returnResponse
    } else {
      throw Soseedy_SeedyAssignmentsClientError.error(c: returnCallResult)
    }
  }

  /// Start the call. Nonblocking.
  fileprivate func start(request: Soseedy_CreateAssignmentRequest,
                         metadata: Metadata,
                         completion: @escaping (Soseedy_Assignment?, CallResult)->())
    throws -> Soseedy_SeedyAssignmentsCreateAssignmentCall {

      let requestData = try request.serializedData()
      try call.start(.unary,
                     metadata:metadata,
                     message:requestData)
      {(callResult) in
        if let responseData = callResult.resultData,
          let response = try? Soseedy_Assignment(serializedData:responseData) {
          completion(response, callResult)
        } else {
          completion(nil, callResult)
        }
      }
      return self
  }

  /// Cancel the call.
  internal func cancel() {
    call.cancel()
  }
}

/// CreateAssignmentOverride (Unary)
internal class Soseedy_SeedyAssignmentsCreateAssignmentOverrideCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/soseedy.SeedyAssignments/CreateAssignmentOverride")
  }

  /// Run the call. Blocks until the reply is received.
  fileprivate func run(request: Soseedy_CreateAssignmentOverrideRequest,
                       metadata: Metadata) throws -> Soseedy_AssignmentOverride {
    let sem = DispatchSemaphore(value: 0)
    var returnCallResult : CallResult!
    var returnResponse : Soseedy_AssignmentOverride?
    _ = try start(request:request, metadata:metadata) {response, callResult in
      returnResponse = response
      returnCallResult = callResult
      sem.signal()
    }
    _ = sem.wait(timeout: DispatchTime.distantFuture)
    if let returnResponse = returnResponse {
      return returnResponse
    } else {
      throw Soseedy_SeedyAssignmentsClientError.error(c: returnCallResult)
    }
  }

  /// Start the call. Nonblocking.
  fileprivate func start(request: Soseedy_CreateAssignmentOverrideRequest,
                         metadata: Metadata,
                         completion: @escaping (Soseedy_AssignmentOverride?, CallResult)->())
    throws -> Soseedy_SeedyAssignmentsCreateAssignmentOverrideCall {

      let requestData = try request.serializedData()
      try call.start(.unary,
                     metadata:metadata,
                     message:requestData)
      {(callResult) in
        if let responseData = callResult.resultData,
          let response = try? Soseedy_AssignmentOverride(serializedData:responseData) {
          completion(response, callResult)
        } else {
          completion(nil, callResult)
        }
      }
      return self
  }

  /// Cancel the call.
  internal func cancel() {
    call.cancel()
  }
}

/// SubmitCourseAssignment (Unary)
internal class Soseedy_SeedyAssignmentsSubmitCourseAssignmentCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/soseedy.SeedyAssignments/SubmitCourseAssignment")
  }

  /// Run the call. Blocks until the reply is received.
  fileprivate func run(request: Soseedy_SubmitCourseAssignmentRequest,
                       metadata: Metadata) throws -> Soseedy_CourseAssignmentSubmission {
    let sem = DispatchSemaphore(value: 0)
    var returnCallResult : CallResult!
    var returnResponse : Soseedy_CourseAssignmentSubmission?
    _ = try start(request:request, metadata:metadata) {response, callResult in
      returnResponse = response
      returnCallResult = callResult
      sem.signal()
    }
    _ = sem.wait(timeout: DispatchTime.distantFuture)
    if let returnResponse = returnResponse {
      return returnResponse
    } else {
      throw Soseedy_SeedyAssignmentsClientError.error(c: returnCallResult)
    }
  }

  /// Start the call. Nonblocking.
  fileprivate func start(request: Soseedy_SubmitCourseAssignmentRequest,
                         metadata: Metadata,
                         completion: @escaping (Soseedy_CourseAssignmentSubmission?, CallResult)->())
    throws -> Soseedy_SeedyAssignmentsSubmitCourseAssignmentCall {

      let requestData = try request.serializedData()
      try call.start(.unary,
                     metadata:metadata,
                     message:requestData)
      {(callResult) in
        if let responseData = callResult.resultData,
          let response = try? Soseedy_CourseAssignmentSubmission(serializedData:responseData) {
          completion(response, callResult)
        } else {
          completion(nil, callResult)
        }
      }
      return self
  }

  /// Cancel the call.
  internal func cancel() {
    call.cancel()
  }
}

/// CreateCourseAssignmentSubmissionComment (Unary)
internal class Soseedy_SeedyAssignmentsCreateCourseAssignmentSubmissionCommentCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/soseedy.SeedyAssignments/CreateCourseAssignmentSubmissionComment")
  }

  /// Run the call. Blocks until the reply is received.
  fileprivate func run(request: Soseedy_CreateCourseAssignmentCommentRequest,
                       metadata: Metadata) throws -> Soseedy_CourseAssignmentSubmission {
    let sem = DispatchSemaphore(value: 0)
    var returnCallResult : CallResult!
    var returnResponse : Soseedy_CourseAssignmentSubmission?
    _ = try start(request:request, metadata:metadata) {response, callResult in
      returnResponse = response
      returnCallResult = callResult
      sem.signal()
    }
    _ = sem.wait(timeout: DispatchTime.distantFuture)
    if let returnResponse = returnResponse {
      return returnResponse
    } else {
      throw Soseedy_SeedyAssignmentsClientError.error(c: returnCallResult)
    }
  }

  /// Start the call. Nonblocking.
  fileprivate func start(request: Soseedy_CreateCourseAssignmentCommentRequest,
                         metadata: Metadata,
                         completion: @escaping (Soseedy_CourseAssignmentSubmission?, CallResult)->())
    throws -> Soseedy_SeedyAssignmentsCreateCourseAssignmentSubmissionCommentCall {

      let requestData = try request.serializedData()
      try call.start(.unary,
                     metadata:metadata,
                     message:requestData)
      {(callResult) in
        if let responseData = callResult.resultData,
          let response = try? Soseedy_CourseAssignmentSubmission(serializedData:responseData) {
          completion(response, callResult)
        } else {
          completion(nil, callResult)
        }
      }
      return self
  }

  /// Cancel the call.
  internal func cancel() {
    call.cancel()
  }
}

/// SeedAssignments (Unary)
internal class Soseedy_SeedyAssignmentsSeedAssignmentsCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/soseedy.SeedyAssignments/SeedAssignments")
  }

  /// Run the call. Blocks until the reply is received.
  fileprivate func run(request: Soseedy_SeedAssignmentRequest,
                       metadata: Metadata) throws -> Soseedy_Assignments {
    let sem = DispatchSemaphore(value: 0)
    var returnCallResult : CallResult!
    var returnResponse : Soseedy_Assignments?
    _ = try start(request:request, metadata:metadata) {response, callResult in
      returnResponse = response
      returnCallResult = callResult
      sem.signal()
    }
    _ = sem.wait(timeout: DispatchTime.distantFuture)
    if let returnResponse = returnResponse {
      return returnResponse
    } else {
      throw Soseedy_SeedyAssignmentsClientError.error(c: returnCallResult)
    }
  }

  /// Start the call. Nonblocking.
  fileprivate func start(request: Soseedy_SeedAssignmentRequest,
                         metadata: Metadata,
                         completion: @escaping (Soseedy_Assignments?, CallResult)->())
    throws -> Soseedy_SeedyAssignmentsSeedAssignmentsCall {

      let requestData = try request.serializedData()
      try call.start(.unary,
                     metadata:metadata,
                     message:requestData)
      {(callResult) in
        if let responseData = callResult.resultData,
          let response = try? Soseedy_Assignments(serializedData:responseData) {
          completion(response, callResult)
        } else {
          completion(nil, callResult)
        }
      }
      return self
  }

  /// Cancel the call.
  internal func cancel() {
    call.cancel()
  }
}

/// SeedAssignmentSubmission (Unary)
internal class Soseedy_SeedyAssignmentsSeedAssignmentSubmissionCall {
  private var call : Call

  /// Create a call.
  fileprivate init(_ channel: Channel) {
    self.call = channel.makeCall("/soseedy.SeedyAssignments/SeedAssignmentSubmission")
  }

  /// Run the call. Blocks until the reply is received.
  fileprivate func run(request: Soseedy_SeedAssignmentSubmissionRequest,
                       metadata: Metadata) throws -> Soseedy_SeededCourseAssignmentSubmissions {
    let sem = DispatchSemaphore(value: 0)
    var returnCallResult : CallResult!
    var returnResponse : Soseedy_SeededCourseAssignmentSubmissions?
    _ = try start(request:request, metadata:metadata) {response, callResult in
      returnResponse = response
      returnCallResult = callResult
      sem.signal()
    }
    _ = sem.wait(timeout: DispatchTime.distantFuture)
    if let returnResponse = returnResponse {
      return returnResponse
    } else {
      throw Soseedy_SeedyAssignmentsClientError.error(c: returnCallResult)
    }
  }

  /// Start the call. Nonblocking.
  fileprivate func start(request: Soseedy_SeedAssignmentSubmissionRequest,
                         metadata: Metadata,
                         completion: @escaping (Soseedy_SeededCourseAssignmentSubmissions?, CallResult)->())
    throws -> Soseedy_SeedyAssignmentsSeedAssignmentSubmissionCall {

      let requestData = try request.serializedData()
      try call.start(.unary,
                     metadata:metadata,
                     message:requestData)
      {(callResult) in
        if let responseData = callResult.resultData,
          let response = try? Soseedy_SeededCourseAssignmentSubmissions(serializedData:responseData) {
          completion(response, callResult)
        } else {
          completion(nil, callResult)
        }
      }
      return self
  }

  /// Cancel the call.
  internal func cancel() {
    call.cancel()
  }
}

/// Call methods of this class to make API calls.
internal class Soseedy_SeedyAssignmentsService {
  public var channel: Channel

  /// This metadata will be sent with all requests.
  internal var metadata : Metadata

  /// This property allows the service host name to be overridden.
  /// For example, it can be used to make calls to "localhost:8080"
  /// appear to be to "example.com".
  internal var host : String {
    get {
      return self.channel.host
    }
    set {
      self.channel.host = newValue
    }
  }

  /// Create a client.
  internal init(address: String, secure: Bool = true) {
    gRPC.initialize()
    channel = Channel(address:address, secure:secure)
    metadata = Metadata()
  }

  /// Create a client that makes secure connections with a custom certificate and (optional) hostname.
  internal init(address: String, certificates: String, host: String?) {
    gRPC.initialize()
    channel = Channel(address:address, certificates:certificates, host:host)
    metadata = Metadata()
  }

  /// Synchronous. Unary.
  internal func createassignment(_ request: Soseedy_CreateAssignmentRequest)
    throws
    -> Soseedy_Assignment {
      return try Soseedy_SeedyAssignmentsCreateAssignmentCall(channel).run(request:request, metadata:metadata)
  }
  /// Asynchronous. Unary.
  internal func createassignment(_ request: Soseedy_CreateAssignmentRequest,
                  completion: @escaping (Soseedy_Assignment?, CallResult)->())
    throws
    -> Soseedy_SeedyAssignmentsCreateAssignmentCall {
      return try Soseedy_SeedyAssignmentsCreateAssignmentCall(channel).start(request:request,
                                                 metadata:metadata,
                                                 completion:completion)
  }
  /// Synchronous. Unary.
  internal func createassignmentoverride(_ request: Soseedy_CreateAssignmentOverrideRequest)
    throws
    -> Soseedy_AssignmentOverride {
      return try Soseedy_SeedyAssignmentsCreateAssignmentOverrideCall(channel).run(request:request, metadata:metadata)
  }
  /// Asynchronous. Unary.
  internal func createassignmentoverride(_ request: Soseedy_CreateAssignmentOverrideRequest,
                  completion: @escaping (Soseedy_AssignmentOverride?, CallResult)->())
    throws
    -> Soseedy_SeedyAssignmentsCreateAssignmentOverrideCall {
      return try Soseedy_SeedyAssignmentsCreateAssignmentOverrideCall(channel).start(request:request,
                                                 metadata:metadata,
                                                 completion:completion)
  }
  /// Synchronous. Unary.
  internal func submitcourseassignment(_ request: Soseedy_SubmitCourseAssignmentRequest)
    throws
    -> Soseedy_CourseAssignmentSubmission {
      return try Soseedy_SeedyAssignmentsSubmitCourseAssignmentCall(channel).run(request:request, metadata:metadata)
  }
  /// Asynchronous. Unary.
  internal func submitcourseassignment(_ request: Soseedy_SubmitCourseAssignmentRequest,
                  completion: @escaping (Soseedy_CourseAssignmentSubmission?, CallResult)->())
    throws
    -> Soseedy_SeedyAssignmentsSubmitCourseAssignmentCall {
      return try Soseedy_SeedyAssignmentsSubmitCourseAssignmentCall(channel).start(request:request,
                                                 metadata:metadata,
                                                 completion:completion)
  }
  /// Synchronous. Unary.
  internal func createcourseassignmentsubmissioncomment(_ request: Soseedy_CreateCourseAssignmentCommentRequest)
    throws
    -> Soseedy_CourseAssignmentSubmission {
      return try Soseedy_SeedyAssignmentsCreateCourseAssignmentSubmissionCommentCall(channel).run(request:request, metadata:metadata)
  }
  /// Asynchronous. Unary.
  internal func createcourseassignmentsubmissioncomment(_ request: Soseedy_CreateCourseAssignmentCommentRequest,
                  completion: @escaping (Soseedy_CourseAssignmentSubmission?, CallResult)->())
    throws
    -> Soseedy_SeedyAssignmentsCreateCourseAssignmentSubmissionCommentCall {
      return try Soseedy_SeedyAssignmentsCreateCourseAssignmentSubmissionCommentCall(channel).start(request:request,
                                                 metadata:metadata,
                                                 completion:completion)
  }
  /// Synchronous. Unary.
  internal func seedassignments(_ request: Soseedy_SeedAssignmentRequest)
    throws
    -> Soseedy_Assignments {
      return try Soseedy_SeedyAssignmentsSeedAssignmentsCall(channel).run(request:request, metadata:metadata)
  }
  /// Asynchronous. Unary.
  internal func seedassignments(_ request: Soseedy_SeedAssignmentRequest,
                  completion: @escaping (Soseedy_Assignments?, CallResult)->())
    throws
    -> Soseedy_SeedyAssignmentsSeedAssignmentsCall {
      return try Soseedy_SeedyAssignmentsSeedAssignmentsCall(channel).start(request:request,
                                                 metadata:metadata,
                                                 completion:completion)
  }
  /// Synchronous. Unary.
  internal func seedassignmentsubmission(_ request: Soseedy_SeedAssignmentSubmissionRequest)
    throws
    -> Soseedy_SeededCourseAssignmentSubmissions {
      return try Soseedy_SeedyAssignmentsSeedAssignmentSubmissionCall(channel).run(request:request, metadata:metadata)
  }
  /// Asynchronous. Unary.
  internal func seedassignmentsubmission(_ request: Soseedy_SeedAssignmentSubmissionRequest,
                  completion: @escaping (Soseedy_SeededCourseAssignmentSubmissions?, CallResult)->())
    throws
    -> Soseedy_SeedyAssignmentsSeedAssignmentSubmissionCall {
      return try Soseedy_SeedyAssignmentsSeedAssignmentSubmissionCall(channel).start(request:request,
                                                 metadata:metadata,
                                                 completion:completion)
  }
}


/// Type for errors thrown from generated server code.
internal enum Soseedy_SeedyAssignmentsServerError : Error {
  case endOfStream
}

/// To build a server, implement a class that conforms to this protocol.
internal protocol Soseedy_SeedyAssignmentsProvider {
  func createassignment(request : Soseedy_CreateAssignmentRequest, session : Soseedy_SeedyAssignmentsCreateAssignmentSession) throws -> Soseedy_Assignment
  func createassignmentoverride(request : Soseedy_CreateAssignmentOverrideRequest, session : Soseedy_SeedyAssignmentsCreateAssignmentOverrideSession) throws -> Soseedy_AssignmentOverride
  func submitcourseassignment(request : Soseedy_SubmitCourseAssignmentRequest, session : Soseedy_SeedyAssignmentsSubmitCourseAssignmentSession) throws -> Soseedy_CourseAssignmentSubmission
  func createcourseassignmentsubmissioncomment(request : Soseedy_CreateCourseAssignmentCommentRequest, session : Soseedy_SeedyAssignmentsCreateCourseAssignmentSubmissionCommentSession) throws -> Soseedy_CourseAssignmentSubmission
  func seedassignments(request : Soseedy_SeedAssignmentRequest, session : Soseedy_SeedyAssignmentsSeedAssignmentsSession) throws -> Soseedy_Assignments
  func seedassignmentsubmission(request : Soseedy_SeedAssignmentSubmissionRequest, session : Soseedy_SeedyAssignmentsSeedAssignmentSubmissionSession) throws -> Soseedy_SeededCourseAssignmentSubmissions
}

/// Common properties available in each service session.
internal class Soseedy_SeedyAssignmentsSession {
  fileprivate var handler : gRPC.Handler
  internal var requestMetadata : Metadata { return handler.requestMetadata }

  internal var statusCode : Int = 0
  internal var statusMessage : String = "OK"
  internal var initialMetadata : Metadata = Metadata()
  internal var trailingMetadata : Metadata = Metadata()

  fileprivate init(handler:gRPC.Handler) {
    self.handler = handler
  }
}

// CreateAssignment (Unary)
internal class Soseedy_SeedyAssignmentsCreateAssignmentSession : Soseedy_SeedyAssignmentsSession {
  private var provider : Soseedy_SeedyAssignmentsProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Soseedy_SeedyAssignmentsProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Soseedy_CreateAssignmentRequest(serializedData:requestData)
        let replyMessage = try self.provider.createassignment(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// CreateAssignmentOverride (Unary)
internal class Soseedy_SeedyAssignmentsCreateAssignmentOverrideSession : Soseedy_SeedyAssignmentsSession {
  private var provider : Soseedy_SeedyAssignmentsProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Soseedy_SeedyAssignmentsProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Soseedy_CreateAssignmentOverrideRequest(serializedData:requestData)
        let replyMessage = try self.provider.createassignmentoverride(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// SubmitCourseAssignment (Unary)
internal class Soseedy_SeedyAssignmentsSubmitCourseAssignmentSession : Soseedy_SeedyAssignmentsSession {
  private var provider : Soseedy_SeedyAssignmentsProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Soseedy_SeedyAssignmentsProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Soseedy_SubmitCourseAssignmentRequest(serializedData:requestData)
        let replyMessage = try self.provider.submitcourseassignment(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// CreateCourseAssignmentSubmissionComment (Unary)
internal class Soseedy_SeedyAssignmentsCreateCourseAssignmentSubmissionCommentSession : Soseedy_SeedyAssignmentsSession {
  private var provider : Soseedy_SeedyAssignmentsProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Soseedy_SeedyAssignmentsProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Soseedy_CreateCourseAssignmentCommentRequest(serializedData:requestData)
        let replyMessage = try self.provider.createcourseassignmentsubmissioncomment(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// SeedAssignments (Unary)
internal class Soseedy_SeedyAssignmentsSeedAssignmentsSession : Soseedy_SeedyAssignmentsSession {
  private var provider : Soseedy_SeedyAssignmentsProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Soseedy_SeedyAssignmentsProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Soseedy_SeedAssignmentRequest(serializedData:requestData)
        let replyMessage = try self.provider.seedassignments(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}

// SeedAssignmentSubmission (Unary)
internal class Soseedy_SeedyAssignmentsSeedAssignmentSubmissionSession : Soseedy_SeedyAssignmentsSession {
  private var provider : Soseedy_SeedyAssignmentsProvider

  /// Create a session.
  fileprivate init(handler:gRPC.Handler, provider: Soseedy_SeedyAssignmentsProvider) {
    self.provider = provider
    super.init(handler:handler)
  }

  /// Run the session. Internal.
  fileprivate func run(queue:DispatchQueue) throws {
    try handler.receiveMessage(initialMetadata:initialMetadata) {(requestData) in
      if let requestData = requestData {
        let requestMessage = try Soseedy_SeedAssignmentSubmissionRequest(serializedData:requestData)
        let replyMessage = try self.provider.seedassignmentsubmission(request:requestMessage, session: self)
        try self.handler.sendResponse(message:replyMessage.serializedData(),
                                      statusCode:self.statusCode,
                                      statusMessage:self.statusMessage,
                                      trailingMetadata:self.trailingMetadata)
      }
    }
  }
}


/// Main server for generated service
internal class Soseedy_SeedyAssignmentsServer {
  private var address: String
  private var server: gRPC.Server
  private var provider: Soseedy_SeedyAssignmentsProvider?

  /// Create a server that accepts insecure connections.
  internal init(address:String,
              provider:Soseedy_SeedyAssignmentsProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    self.server = gRPC.Server(address:address)
  }

  /// Create a server that accepts secure connections.
  internal init?(address:String,
               certificateURL:URL,
               keyURL:URL,
               provider:Soseedy_SeedyAssignmentsProvider) {
    gRPC.initialize()
    self.address = address
    self.provider = provider
    guard
      let certificate = try? String(contentsOf: certificateURL, encoding: .utf8),
      let key = try? String(contentsOf: keyURL, encoding: .utf8)
      else {
        return nil
    }
    self.server = gRPC.Server(address:address, key:key, certs:certificate)
  }

  /// Start the server.
  internal func start(queue:DispatchQueue = DispatchQueue.global()) {
    guard let provider = self.provider else {
      fatalError() // the server requires a provider
    }
    server.run {(handler) in
      print("Server received request to " + handler.host
        + " calling " + handler.method
        + " from " + handler.caller
        + " with " + String(describing:handler.requestMetadata) )

      do {
        switch handler.method {
        case "/soseedy.SeedyAssignments/CreateAssignment":
          try Soseedy_SeedyAssignmentsCreateAssignmentSession(handler:handler, provider:provider).run(queue:queue)
        case "/soseedy.SeedyAssignments/CreateAssignmentOverride":
          try Soseedy_SeedyAssignmentsCreateAssignmentOverrideSession(handler:handler, provider:provider).run(queue:queue)
        case "/soseedy.SeedyAssignments/SubmitCourseAssignment":
          try Soseedy_SeedyAssignmentsSubmitCourseAssignmentSession(handler:handler, provider:provider).run(queue:queue)
        case "/soseedy.SeedyAssignments/CreateCourseAssignmentSubmissionComment":
          try Soseedy_SeedyAssignmentsCreateCourseAssignmentSubmissionCommentSession(handler:handler, provider:provider).run(queue:queue)
        case "/soseedy.SeedyAssignments/SeedAssignments":
          try Soseedy_SeedyAssignmentsSeedAssignmentsSession(handler:handler, provider:provider).run(queue:queue)
        case "/soseedy.SeedyAssignments/SeedAssignmentSubmission":
          try Soseedy_SeedyAssignmentsSeedAssignmentSubmissionSession(handler:handler, provider:provider).run(queue:queue)
        default:
          break // handle unknown requests
        }
      } catch (let error) {
        print("Server error: \(error)")
      }
    }
  }
}

