// @flow

import template from '../utils/template'

export const navigator: Template<ReactNavigator> = template({
  showModal: () => {},
  dismissModal: () => {},
  setOnNavigatorEvent: () => {},
  pop: () => {},
  push: () => {},
})
