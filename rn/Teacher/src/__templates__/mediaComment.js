/* @flow */

import template, { type Template } from '../utils/template'

export const mediaComment: Template<MediaComment> = template({
  url: 'https://notorious.com/audio',
  media_type: 'audio',
  media_id: '1',
  display_name: null,
})
