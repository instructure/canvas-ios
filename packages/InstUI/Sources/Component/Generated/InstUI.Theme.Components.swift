//
// This file is part of Canvas.
// Copyright (C) 2026-present  Instructure, Inc.
//
// This program is free software: you can redistribute it and/or modify
// it under the terms of the GNU Affero General Public License as
// published by the Free Software Foundation, either version 3 of the
// License, or (at your option) any later version.
//
// This program is distributed in the hope that it will be useful,
// but WITHOUT ANY WARRANTY; without even the implied warranty of
// MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
// GNU Affero General Public License for more details.
//
// You should have received a copy of the GNU Affero General Public License
// along with this program.  If not, see <https://www.gnu.org/licenses/>.
//

import CoreGraphics
import Foundation
import SwiftUI

// DO NOT EDIT — Auto-generated by `yarn build-instui`
// swiftlint:disable function_body_length

extension InstUI.Theme {

    public struct Components: Sendable {
        public let aiInformation: InstUI.Component.AiInformation
        public let alert: InstUI.Component.Alert
        public let appNav: InstUI.Component.AppNav
        public let avatar: InstUI.Component.Avatar
        public let badge: InstUI.Component.Badge
        public let baseButton: InstUI.Component.BaseButton
        public let billboard: InstUI.Component.Billboard
        public let breadcrumb: InstUI.Component.Breadcrumb
        public let byline: InstUI.Component.Byline
        public let calendar: InstUI.Component.Calendar
        public let checkbox: InstUI.Component.Checkbox
        public let colorContrast: InstUI.Component.ColorContrast
        public let colorIndicator: InstUI.Component.ColorIndicator
        public let palette: InstUI.Component.Palette
        public let colorPicker: InstUI.Component.ColorPicker
        public let colorPreset: InstUI.Component.ColorPreset
        public let contextView: InstUI.Component.ContextView
        public let dataPermissionLevels: InstUI.Component.DataPermissionLevels
        public let drilldown: InstUI.Component.Drilldown
        public let fileDrop: InstUI.Component.FileDrop
        public let flex: InstUI.Component.Flex
        public let formFieldLayout: InstUI.Component.FormFieldLayout
        public let formFieldMessage: InstUI.Component.FormFieldMessage
        public let grid: InstUI.Component.Grid
        public let heading: InstUI.Component.Heading
        public let icon: InstUI.Component.Icon
        public let inlineListItem: InstUI.Component.InlineListItem
        public let link: InstUI.Component.Link
        public let listItem: InstUI.Component.ListItem
        public let mask: InstUI.Component.Mask
        public let menu: InstUI.Component.Menu
        public let metric: InstUI.Component.Metric
        public let modal: InstUI.Component.Modal
        public let nutritionFacts: InstUI.Component.NutritionFacts
        public let options: InstUI.Component.Options
        public let pagination: InstUI.Component.Pagination
        public let pill: InstUI.Component.Pill
        public let popover: InstUI.Component.Popover
        public let progressBar: InstUI.Component.ProgressBar
        public let progressCircle: InstUI.Component.ProgressCircle
        public let radioInput: InstUI.Component.RadioInput
        public let rangeInput: InstUI.Component.RangeInput
        public let ratingIcon: InstUI.Component.RatingIcon
        public let select: InstUI.Component.Select
        public let sharedTokens: InstUI.Component.SharedTokens
        public let sideNavBar: InstUI.Component.SideNavBar
        public let sideNavBarNew: InstUI.Component.SideNavBarNew
        public let sourceCodeEditor: InstUI.Component.SourceCodeEditor
        public let spinner: InstUI.Component.Spinner
        public let table: InstUI.Component.Table
        public let tabs: InstUI.Component.Tabs
        public let tag: InstUI.Component.Tag
        public let text: InstUI.Component.Text
        public let textArea: InstUI.Component.TextArea
        public let textInput: InstUI.Component.TextInput
        public let toggle: InstUI.Component.Toggle
        public let toggleDetails: InstUI.Component.ToggleDetails
        public let toggleGroup: InstUI.Component.ToggleGroup
        public let tooltip: InstUI.Component.Tooltip
        public let topNavBarActionItems: InstUI.Component.TopNavBarActionItems
        public let tray: InstUI.Component.Tray
        public let treeBrowser: InstUI.Component.TreeBrowser
        public let truncateText: InstUI.Component.TruncateText
        public let view: InstUI.Component.View
    }
}

extension InstUI.Theme.Components {

    static func load(
        colors: InstUI.Semantic.Color,
        size: InstUI.Semantic.Size,
        spacing: InstUI.Semantic.Spacing,
        borderRadius: InstUI.Semantic.BorderRadius,
        borderWidth: InstUI.Semantic.BorderWidth,
        fontSize: InstUI.Semantic.FontSize,
        opacity: InstUI.Semantic.Opacity,
        fontWeights: InstUI.Semantic.FontWeight,
        fontFamilies: InstUI.Semantic.FontFamily
    ) throws -> Self {
        let base = InstUI.ComponentResolver(
            colors: colors,
            size: size,
            spacing: spacing,
            borderRadius: borderRadius,
            borderWidth: borderWidth,
            fontSize: fontSize,
            opacity: opacity,
            fontWeights: fontWeights,
            fontFamilies: fontFamilies,
            leaves: [:]
        )
        func resolveComponent<T>(
            _ resource: String,
            _ section: String,
            _ build: (InstUI.ComponentResolver) throws -> T
        ) throws -> T {
            guard let url = Bundle.module.url(forResource: resource, withExtension: "json") else {
                throw InstUI.TokenLoadError.missingFile("\(resource).json not found in bundle")
            }
            let data = try Data(contentsOf: url)
            let leaves = try InstUI.TokenExtractor.extractLeaves(from: data, section: section)
            return try build(base.withLeaves(leaves))
        }

        let aiInformation = try resolveComponent("AiInformation", "aiInformation") { resolver in
            try InstUI.Component.AiInformation.build(dimension: resolver.dimension, color: resolver.color)
        }
        let alert = try resolveComponent("Alert", "alert") { resolver in
            try InstUI.Component.Alert.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let appNav = try resolveComponent("AppNav", "appNav") { resolver in
            try InstUI.Component.AppNav.build(dimension: resolver.dimension, color: resolver.color, fontFamily: resolver.fontFamily)
        }
        let avatar = try resolveComponent("Avatar", "avatar") { resolver in
            try InstUI.Component.Avatar.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let badge = try resolveComponent("Badge", "badge") { resolver in
            try InstUI.Component.Badge.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let baseButton = try resolveComponent("BaseButton", "baseButton") { resolver in
            try InstUI.Component.BaseButton.build(dimension: resolver.dimension, color: resolver.color, opacity: resolver.opacity, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let billboard = try resolveComponent("Billboard", "billboard") { resolver in
            try InstUI.Component.Billboard.build(dimension: resolver.dimension, color: resolver.color, fontFamily: resolver.fontFamily)
        }
        let breadcrumb = try resolveComponent("Breadcrumb", "breadcrumb") { resolver in
            try InstUI.Component.Breadcrumb.build(dimension: resolver.dimension)
        }
        let byline = try resolveComponent("Byline", "byline") { resolver in
            try InstUI.Component.Byline.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let calendar = try resolveComponent("Calendar", "calendar") { resolver in
            try InstUI.Component.Calendar.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let checkbox = try resolveComponent("Checkbox", "checkbox") { resolver in
            try InstUI.Component.Checkbox.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let colorContrast = try resolveComponent("ColorContrast", "colorContrast") { resolver in
            try InstUI.Component.ColorContrast.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let colorIndicator = try resolveComponent("ColorIndicator", "colorIndicator") { resolver in
            try InstUI.Component.ColorIndicator.build(dimension: resolver.dimension, color: resolver.color)
        }
        let palette = try resolveComponent("ColorMixer", "palette") { resolver in
            try InstUI.Component.Palette.build(dimension: resolver.dimension, color: resolver.color)
        }
        let colorPicker = try resolveComponent("ColorPicker", "colorPicker") { resolver in
            try InstUI.Component.ColorPicker.build(dimension: resolver.dimension, color: resolver.color)
        }
        let colorPreset = try resolveComponent("ColorPreset", "colorPreset") { resolver in
            try InstUI.Component.ColorPreset.build(dimension: resolver.dimension, color: resolver.color)
        }
        let contextView = try resolveComponent("ContextView", "contextView") { resolver in
            try InstUI.Component.ContextView.build(dimension: resolver.dimension, color: resolver.color)
        }
        let dataPermissionLevels = try resolveComponent("DataPermissionLevels", "dataPermissionLevels") { resolver in
            try InstUI.Component.DataPermissionLevels.build(dimension: resolver.dimension, color: resolver.color)
        }
        let drilldown = try resolveComponent("Drilldown", "drilldown") { resolver in
            try InstUI.Component.Drilldown.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight)
        }
        let fileDrop = try resolveComponent("FileDrop", "fileDrop") { resolver in
            try InstUI.Component.FileDrop.build(dimension: resolver.dimension, color: resolver.color)
        }
        let flex = try resolveComponent("Flex", "flex") { resolver in
            try InstUI.Component.Flex.build(fontFamily: resolver.fontFamily)
        }
        let formFieldLayout = try resolveComponent("FormFieldLayout", "formFieldLayout") { resolver in
            try InstUI.Component.FormFieldLayout.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let formFieldMessage = try resolveComponent("FormFieldMessage", "formFieldMessage") { resolver in
            try InstUI.Component.FormFieldMessage.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let grid = try resolveComponent("Grid", "grid") { resolver in
            try InstUI.Component.Grid.build(dimension: resolver.dimension)
        }
        let heading = try resolveComponent("Heading", "heading") { resolver in
            try InstUI.Component.Heading.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let icon = try resolveComponent("Icon", "icon") { resolver in
            try InstUI.Component.Icon.build(dimension: resolver.dimension, color: resolver.color)
        }
        let inlineListItem = try resolveComponent("InlineList", "inlineListItem") { resolver in
            try InstUI.Component.InlineListItem.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let link = try resolveComponent("Link", "link") { resolver in
            try InstUI.Component.Link.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let listItem = try resolveComponent("List", "listItem") { resolver in
            try InstUI.Component.ListItem.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let mask = try resolveComponent("Mask", "mask") { resolver in
            try InstUI.Component.Mask.build(color: resolver.color)
        }
        let menu = try resolveComponent("Menu", "menu") { resolver in
            try InstUI.Component.Menu.build(dimension: resolver.dimension)
        }
        let metric = try resolveComponent("Metric", "metric") { resolver in
            try InstUI.Component.Metric.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let modal = try resolveComponent("Modal", "modal") { resolver in
            try InstUI.Component.Modal.build(dimension: resolver.dimension, color: resolver.color, fontFamily: resolver.fontFamily)
        }
        let nutritionFacts = try resolveComponent("NutritionFacts", "nutritionFacts") { resolver in
            try InstUI.Component.NutritionFacts.build(dimension: resolver.dimension, color: resolver.color)
        }
        let options = try resolveComponent("Options", "options") { resolver in
            try InstUI.Component.Options.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight)
        }
        let pagination = try resolveComponent("Pagination", "pagination") { resolver in
            try InstUI.Component.Pagination.build(dimension: resolver.dimension)
        }
        let pill = try resolveComponent("Pill", "pill") { resolver in
            try InstUI.Component.Pill.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let popover = try resolveComponent("Popover", "popover") { resolver in
            try InstUI.Component.Popover.build(dimension: resolver.dimension, color: resolver.color)
        }
        let progressBar = try resolveComponent("ProgressBar", "progressBar") { resolver in
            try InstUI.Component.ProgressBar.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let progressCircle = try resolveComponent("ProgressCircle", "progressCircle") { resolver in
            try InstUI.Component.ProgressCircle.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let radioInput = try resolveComponent("RadioInput", "radioInput") { resolver in
            try InstUI.Component.RadioInput.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let rangeInput = try resolveComponent("RangeInput", "rangeInput") { resolver in
            try InstUI.Component.RangeInput.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let ratingIcon = try resolveComponent("RatingIcon", "ratingIcon") { resolver in
            try InstUI.Component.RatingIcon.build(dimension: resolver.dimension, color: resolver.color)
        }
        let select = try resolveComponent("Select", "select") { resolver in
            try InstUI.Component.Select.build(dimension: resolver.dimension)
        }
        let sharedTokens = try resolveComponent("SharedTokens", "sharedTokens") { resolver in
            try InstUI.Component.SharedTokens.build(dimension: resolver.dimension, color: resolver.color)
        }
        let sideNavBar = try resolveComponent("SideNavBar", "sideNavBar") { resolver in
            try InstUI.Component.SideNavBar.build(dimension: resolver.dimension, color: resolver.color)
        }
        let sideNavBarNew = try resolveComponent("SideNavBarNew", "sideNavBarNew") { resolver in
            try InstUI.Component.SideNavBarNew.build(dimension: resolver.dimension, color: resolver.color)
        }
        let sourceCodeEditor = try resolveComponent("SourceCodeEditor", "sourceCodeEditor") { resolver in
            try InstUI.Component.SourceCodeEditor.build(dimension: resolver.dimension, color: resolver.color, fontFamily: resolver.fontFamily)
        }
        let spinner = try resolveComponent("Spinner", "spinner") { resolver in
            try InstUI.Component.Spinner.build(dimension: resolver.dimension, color: resolver.color)
        }
        let table = try resolveComponent("Table", "table") { resolver in
            try InstUI.Component.Table.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let tabs = try resolveComponent("Tabs", "tabs") { resolver in
            try InstUI.Component.Tabs.build(dimension: resolver.dimension, color: resolver.color)
        }
        let tag = try resolveComponent("Tag", "tag") { resolver in
            try InstUI.Component.Tag.build(dimension: resolver.dimension, color: resolver.color, fontFamily: resolver.fontFamily)
        }
        let text = try resolveComponent("Text", "text") { resolver in
            try InstUI.Component.Text.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let textArea = try resolveComponent("TextArea", "textArea") { resolver in
            try InstUI.Component.TextArea.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let textInput = try resolveComponent("TextInput", "textInput") { resolver in
            try InstUI.Component.TextInput.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let toggle = try resolveComponent("Toggle", "toggle") { resolver in
            try InstUI.Component.Toggle.build(dimension: resolver.dimension, color: resolver.color, opacity: resolver.opacity, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let toggleDetails = try resolveComponent("ToggleDetails", "toggleDetails") { resolver in
            try InstUI.Component.ToggleDetails.build(dimension: resolver.dimension, color: resolver.color, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let toggleGroup = try resolveComponent("ToggleGroup", "toggleGroup") { resolver in
            try InstUI.Component.ToggleGroup.build(color: resolver.color)
        }
        let tooltip = try resolveComponent("Tooltip", "tooltip") { resolver in
            try InstUI.Component.Tooltip.build(dimension: resolver.dimension, fontWeight: resolver.fontWeight, fontFamily: resolver.fontFamily)
        }
        let topNavBarActionItems = try resolveComponent("TopNavBar", "topNavBarActionItems") { resolver in
            try InstUI.Component.TopNavBarActionItems.build(dimension: resolver.dimension, fontWeight: resolver.fontWeight)
        }
        let tray = try resolveComponent("Tray", "tray") { resolver in
            try InstUI.Component.Tray.build(dimension: resolver.dimension, color: resolver.color)
        }
        let treeBrowser = try resolveComponent("TreeBrowser", "treeBrowser") { resolver in
            try InstUI.Component.TreeBrowser.build(dimension: resolver.dimension)
        }
        let truncateText = try resolveComponent("TruncateText", "truncateText") { resolver in
            try InstUI.Component.TruncateText.build(fontFamily: resolver.fontFamily)
        }
        let view = try resolveComponent("View", "view") { resolver in
            try InstUI.Component.View.build(color: resolver.color, fontFamily: resolver.fontFamily)
        }

        return Self(
            aiInformation: aiInformation,
            alert: alert,
            appNav: appNav,
            avatar: avatar,
            badge: badge,
            baseButton: baseButton,
            billboard: billboard,
            breadcrumb: breadcrumb,
            byline: byline,
            calendar: calendar,
            checkbox: checkbox,
            colorContrast: colorContrast,
            colorIndicator: colorIndicator,
            palette: palette,
            colorPicker: colorPicker,
            colorPreset: colorPreset,
            contextView: contextView,
            dataPermissionLevels: dataPermissionLevels,
            drilldown: drilldown,
            fileDrop: fileDrop,
            flex: flex,
            formFieldLayout: formFieldLayout,
            formFieldMessage: formFieldMessage,
            grid: grid,
            heading: heading,
            icon: icon,
            inlineListItem: inlineListItem,
            link: link,
            listItem: listItem,
            mask: mask,
            menu: menu,
            metric: metric,
            modal: modal,
            nutritionFacts: nutritionFacts,
            options: options,
            pagination: pagination,
            pill: pill,
            popover: popover,
            progressBar: progressBar,
            progressCircle: progressCircle,
            radioInput: radioInput,
            rangeInput: rangeInput,
            ratingIcon: ratingIcon,
            select: select,
            sharedTokens: sharedTokens,
            sideNavBar: sideNavBar,
            sideNavBarNew: sideNavBarNew,
            sourceCodeEditor: sourceCodeEditor,
            spinner: spinner,
            table: table,
            tabs: tabs,
            tag: tag,
            text: text,
            textArea: textArea,
            textInput: textInput,
            toggle: toggle,
            toggleDetails: toggleDetails,
            toggleGroup: toggleGroup,
            tooltip: tooltip,
            topNavBarActionItems: topNavBarActionItems,
            tray: tray,
            treeBrowser: treeBrowser,
            truncateText: truncateText,
            view: view
        )
    }

    static func load(using theme: InstUI.Theme) throws -> Self {
        try load(
            colors: theme.colors,
            size: theme.size,
            spacing: theme.spacing,
            borderRadius: theme.borderRadius,
            borderWidth: theme.borderWidth,
            fontSize: theme.fontSize,
            opacity: theme.opacity,
            fontWeights: theme.fontWeights,
            fontFamilies: theme.fontFamilies
        )
    }
}
