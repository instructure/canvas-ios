name: CITest
on:
  push:
    branches:
      - '*'
      - '!master'

jobs:
  citest:
    name: CITest
    runs-on: macOS-10.14
    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 1
      - uses: webfactory/ssh-agent@v0.1.1
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}
      # - name: Brew Install
      #   run: brew install swiftlint
      # - name: SwiftLint
      #   run: ./scripts/runSwiftLint.sh
      # - uses: actions/setup-node@master
      #   with:
      #     node-version: 10.16.0
      # - name: YARN Install
      #   run: yarn
      #   working-directory: ./rn/Teacher
      # - name: YARN Lint
      #   run: yarn lint
      #   working-directory: ./rn/Teacher
      # - name: YARN Test
      #   run: yarn test
      #   working-directory: ./rn/Teacher
      - name: Clone Canvas iOS Secrets
        run: git clone git@github.com:instructure/steps-canvas-ios-secrets.git
        working-directory: ../
      - name: Setup Canvas iOS Secrets
        run: ../steps-canvas-ios-secrets/scripts/setup-dev-secrets.sh
      # - name: Carthage Bootstrap
      #   run: carthage bootstrap --platform iOS
      # - name: CocoaPods Install
      #   run: pod install