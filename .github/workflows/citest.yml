name: CITest
on:
  push:
    branches:
      - '*'
      - '!master'

jobs:
  citest:
    name: CITest
    runs-on: macOS-10.14
    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 1
      # - name: Brew Install
      #   run: brew install swiftlint
      # - name: SwiftLint
      #   run: ./scripts/runSwiftLint.sh
      # - uses: actions/setup-node@master
      #   with:
      #     node-version: 10.16.0
      # - name: YARN Install
      #   run: yarn
      #   working-directory: ./rn/Teacher
      # - name: YARN Lint
      #   run: yarn lint
      #   working-directory: ./rn/Teacher
      # - name: YARN Test
      #   run: yarn test
      #   working-directory: ./rn/Teacher
      - name: Clone Canvas iOS Secrets
        run: git clone https://ios-danger:$GITHUB_ACCESS_TOKEN@github.com/instructure/steps-canvas-ios-secrets
        working-directory: ../
        env:
          GITHUB_ACCESS_TOKEN: ${{ secrets.GITHUB_ACCESS_TOKEN }}
      - name: Setup Canvas iOS Secrets
        run: ../steps-canvas-ios-secrets/scripts/setup-dev-secrets.sh
      # - name: Carthage Bootstrap
      #   run: carthage bootstrap --platform iOS
      # - name: CocoaPods Install
      #   run: pod install