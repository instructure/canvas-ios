struct Quiz {
    let id: Int
    let title: String
    let description: String
    let quizType: String
    let timeLimit: Int
    let showCorrectAnswers: Bool
    let allowedAttempts: Int
    let pointsPossible: Int
    let dueAt: String
    let published: Bool
}
